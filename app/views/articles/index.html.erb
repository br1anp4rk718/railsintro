<ul>
<% @articles.each do |each_article| %>

<li><%= each_article.title %> --- <%= link_to "Show", article_path(each_article) %> --- <%= link_to "DESTROY", article_path(each_article), method: :delete, data: {confirm: "Are you sure??"} %></li>

<% end %>
</ul>

<%= link_to 'New Article' , new_article_path %>
